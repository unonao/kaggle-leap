defaults:
  - base@_here_ # ベースの設定ファイルを指定する

model:
  n_base_channels: 32

scheduler:
  name: ReduceLROnPlateau
  mode: max
  factor: 0.1
  patience: 0
  threshold: 1e-4
  threshold_mode: abs
  cooldown: 2

optimizer: 
  lr: 2e-2

train_data_skip_mod: 1
val_check_interval: 9000
max_epochs: 4
