defaults:
  - base@_here_ # ベースの設定ファイルを指定する

max_epochs: 100
train_data_skip_mod: 4
early_stopping_patience: 3


resume_ckpt_path: output/experiments/021_radam/base_large_data/checkpoints/best_model.ckpt

# Training
optimizer: 
  name: RAdam
  lr: 1e-3
  weight_decay: 0