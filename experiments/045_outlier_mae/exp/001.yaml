defaults:
  - base@_here_ # ベースの設定ファイルを指定する

model:
  n_base_channels: 64
  n_feat_channels: 8

loss:
  mae_rate: 0.1

outlier_std_rate: 40

optimizer: 
  name: AdamW
  lr: 5e-2
  weight_decay: 0.01

train_batch_size: 2
valid_batch_size: 2
accumulate_grad_batches: 2