defaults:
  - base@_here_ # ベースの設定ファイルを指定する

# model
model:
  same_height_hidden_sizes: [128, 64]
  embedding_dim: 10
  dropout: 0.2
  bilinear: False
  n_base_channels: 64


train_data_skip_mod: 1
val_check_interval: 20000
